pub const WIDTH: u16 = 320
pub const HEIGHT: u8 = 240
pub const VRAM_BASE: u24 = 0xD40000

pub fn set_first_pixel(color: u8) {
    asm volatile(in color: u8 as reg8, clobber a, clobber memory) {
        "ld a, {color}"
        "ld (0D40000h), a"
    }
}
