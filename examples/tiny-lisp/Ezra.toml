[project]
name = "tiny-lisp"

[build]
# Agon is the default for `cargo run -- build examples/tiny-lisp/main.ezra`.
# Override it with --target for CP/M, C64, ZX Spectrum, or TI-99/4A.
target = "agonlight-mos-ez80"
executable = "tiny-lisp"
