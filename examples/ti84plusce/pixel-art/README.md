# TI-84 Plus CE Pixel Art

Draws a simple block-art character using custom RGB colors and clipped rectangles, then waits for a key before returning.

```sh
cargo run -- build examples/ti84plusce/pixel-art/src/main.ezra
```

Run the resulting program with `Asm(prgmEZRAPIX)`.
