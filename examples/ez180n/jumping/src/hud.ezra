import ez180n.console
import helpers

// HUD
// ==================================================

global HUD_STATE: u8 = 0

pub fn draw_hud() {
    if HUD_STATE == 0 {
        helpers.write_line(1, 1, "DPAD to move, A to jump")
    }
    if HUD_STATE == 1 {
        helpers.write_line(1, 1, "Level 1 ")
    }

    helpers.write_line(console.WIDTH - 10, 1, helpers.get_lives_string())
}
